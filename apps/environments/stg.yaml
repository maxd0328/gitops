config:
  gitopsTool: argo
  gitopsNamespace: argocd
  prefix: stg
  commonChart:
    repoURL: devops.derbserv.hq/_git/charts
    path: gitops/
    version: HEAD

environment:
  extras:
    keycloak:
      replicas: 1
      hostname: iam.stg.derbserv.hq

    db-cluster:
      replicas: 1
      storageSize: 500Mi
